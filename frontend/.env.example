# URL de base de l'API backend (sans slash final)
# En production : pointer vers le domaine de l'API
# En dev local : laisser vide (proxy Vite gère /api et /uploads)
VITE_API_URL=https://cocktail-app.fr
